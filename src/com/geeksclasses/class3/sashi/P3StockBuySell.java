package com.geeksclasses.class3.sashi;

public class P3StockBuySell {

	public static void main(String args[]){
		int arr[] = {100, 360, 400, 500, 180};
		
		/*int maxProfit=0;
		int buyPrice=0;
		for(int i=0;i<arr.length;i++){
			if(i!=arr.length-1 && buyPrice==0 && arr[i+1]>arr[i]){
				buyPrice=arr[i];
				System.out.println("Buy on Day"+(i+1));
			}
			
			if(i!=arr.length-1 && buyPrice!=0 && arr[i+1] < arr[i]){
				maxProfit = maxProfit + (arr[i] - buyPrice);
				buyPrice=0;
				System.out.println("Sell on Day"+(i+1));
			}
			else if(i==arr.length-1 && buyPrice!=0){
				maxProfit = maxProfit + (arr[i] - buyPrice);
				buyPrice=0;
				System.out.println("Sell on Day"+(i+1));
			}
		}
		
		System.out.println("Max Profit = "+maxProfit);*/
	}
	
	public static class Solver{
		public void findMaxProfitPoints(){
				
		}
	}
}